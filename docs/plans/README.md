# Plans

Execution roadmaps — historical and active — for work tracked against a spec
in `docs/specs/`. A plan records the step-by-step implementation sequence and
its completion state; it is not rewritten once the work ships, so it remains
a true record of what happened and when.

Filename convention: `YYYY-MM-DD-<slug>.md`.

```bash
ls docs/plans/*.md
```
