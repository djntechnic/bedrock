/** Bytes as the operator reads them — the API returns raw bytes. */
export declare function formatBytes(bytes: number): string;
export default function PlatformHealthPanel(): import("react").JSX.Element;
